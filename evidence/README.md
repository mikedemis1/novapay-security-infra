# Evidence

Screenshots and logs captured during apply-test-destroy cycles, kept as proof that a control was actually built and worked — since most resources get destroyed same-day to stay within budget.

Naming: `<resource>-<what-it-shows>.png` (e.g. `secrets-manager-applied.png`).

Before adding a screenshot: crop out AWS Account ID and IAM usernames.
